# name: Scheduled Live Market Scraper
# 
# on:
#   schedule:
#     # 1. The Opening Bracket: Runs every 10 mins from 09:00 to 09:20 Tehran Time (05:30 to 05:50 UTC)
#     - cron: '30,40,50 5 * * 0,1,2,3,4,6'
#     
#     # 2. The Core Block: Runs every 10 mins from 09:30 to 16:20 Tehran Time (06:00 to 12:50 UTC)
#     - cron: '*/10 6-12 * * 0,1,2,3,4,6'
#     
#     # 3. The Closing Bracket: Runs every 10 mins from 16:30 to 17:00 Tehran Time (13:00 to 13:30 UTC)
#     - cron: '0,10,20,30 13 * * 0,1,2,3,4,6'
# 
#   workflow_dispatch: # Keeps your manual trigger button alive
# 
# jobs:
#   run-scraper:
#     runs-on: ubuntu-latest
#     steps:
#       # Updated to v4 to fix the Node.js 20 deprecation warning
#       - name: Checkout Code
#         uses: actions/checkout@v4
# 
#       # Updated to v5 to fix the Node.js 20 deprecation warning
#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.11'
# 
#       - name: Install Dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt
# 
#       - name: Execute Live Scraper Bot
#         env:
#           API_ID: ${{ secrets.API_ID }}
#           API_HASH: ${{ secrets.API_HASH }}
#           TELETHON_SESSION: ${{ secrets.TELETHON_SESSION }}
#           BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
#           CHAT_ID: ${{ secrets.CHAT_ID }}
#           # Forces the Ubuntu runner into Iran Standard Time
#           TZ: "Asia/Tehran" 
#         run: python src/bot.py
